// MySQL disabled intentionally
// This project currently uses MongoDB only

/*
const mysql = require("mysql2");

const conn = mysql.createConnection({
  host: "localhost",
  user: "root",
  password: "simar123_321",
  database: "blog_users"
});

conn.connect(err => {
  if (err) throw err;
  console.log("MySQL connected");
});

module.exports = conn;
*/
